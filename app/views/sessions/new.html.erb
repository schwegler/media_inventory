<h1>Log in</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(url: login_path, scope: :session, class: "standard-form") do |f| %>
      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, class: 'form-control', required: true %>
      </div>

      <div class="form-group" style="margin-top: 15px;">
        <%= f.label :password %>
        <%= f.password_field :password, class: 'form-control', required: true %>
      <div style="margin-top: 15px;">
        <%= f.submit "Log in", class: "sleek-btn sleek-btn-primary", style: "width: 100%; justify-content: center;" %>
      </div>
    <% end %>

    <p style="margin-top: 20px; text-align: center;">
      New user? <%= link_to "Sign up now!", signup_path %>
    </p>

    <hr style="border: 0; border-top: 1px solid rgba(255,255,255,0.08); margin: 2rem 0;">

    <div class="auth-sections-container">
      <div class="auth-section" style="margin-bottom: 2rem;">
        <h2 style="font-size: 1.25rem; text-align: center; margin-bottom: 1.5rem;">🦋 Log in with Bluesky</h2>
        <div style="text-align: center;">
          <%= button_to "Log in via Bluesky", "/auth/atproto", method: :post, data: { turbo: false }, class: "sleek-btn sleek-btn-secondary", style: "margin: 0 auto; width: 100%; background: #0284c7; border-color: #0ea5e9;" %>
        </div>
      </div>

      <div class="auth-section" style="padding: 1.5rem; background: rgba(30, 41, 59, 0.3); border: 1px solid rgba(255, 255, 255, 0.05); border-radius: 12px;">
        <h2 style="font-size: 1.25rem; text-align: center; margin-bottom: 1.5rem;">🐘 Log in with Mastodon</h2>
        <div style="text-align: center;">
          <%= form_tag "/auth/mastodon", method: :post, data: { turbo: false } do %>
            <div class="form-group" style="margin-bottom: 1.5rem;">
              <%= label_tag :mastodon_server, "Mastodon Server URL" %>
              <%= text_field_tag :mastodon_server, nil, class: 'form-control', placeholder: 'e.g. mastodon.social', required: true %>
            </div>
            <%= submit_tag "Log in via Mastodon", class: "sleek-btn sleek-btn-secondary", style: "width: 100%; background: #563acc; border-color: #7c3aed;" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
