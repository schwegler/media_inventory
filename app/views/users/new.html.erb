<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div data-controller="tabs" class="auth-tabs-container">
      <div class="auth-tabs" role="tablist" aria-label="Sign Up Options">
        <button type="button" class="auth-tab active" role="tab" id="tab-signup-email" aria-controls="panel-signup-email" data-tabs-target="tab" data-tab-name="email" data-action="click->tabs#switch keydown->tabs#keydown">Email</button>
        <button type="button" class="auth-tab" role="tab" id="tab-signup-bsky" aria-controls="panel-signup-bsky" data-tabs-target="tab" data-tab-name="bsky-oauth" data-action="click->tabs#switch keydown->tabs#keydown">Bluesky</button>
        <button type="button" class="auth-tab" role="tab" id="tab-signup-mastodon" aria-controls="panel-signup-mastodon" data-tabs-target="tab" data-tab-name="mastodon" data-action="click->tabs#switch keydown->tabs#keydown">Mastodon</button>
      </div>

      <div class="auth-tab-content" role="tabpanel" id="panel-signup-email" aria-labelledby="tab-signup-email" data-tabs-target="content" data-tab-name="email">
        <h2 class="auth-section-title">Sign up with Email</h2>
        <div class="auth-section-body">
          <%= form_with(model: @user, local: true, class: "standard-form") do |f| %>
            <%= render 'shared/error_messages', object: f.object if lookup_context.exists?('shared/_error_messages') %>

            <div class="form-group">
              <%= f.label :name %>
              <%= f.text_field :name, class: 'form-control', required: true %>
            </div>

            <div class="form-group" style="margin-top: 15px;">
              <%= f.label :email %>
              <%= f.email_field :email, class: 'form-control', required: true %>
            </div>

            <div class="form-group" style="margin-top: 15px;">
              <%= f.label :password %>
              <%= f.password_field :password, class: 'form-control', required: true %>
            </div>

            <div class="form-group" style="margin-top: 15px;">
              <%= f.label :password_confirmation, "Confirmation" %>
              <%= f.password_field :password_confirmation, class: 'form-control', required: true %>
            </div>

            <div class="submit-group" style="margin-top: 20px;">
              <%= f.submit "Sign up", class: "sleek-btn sleek-btn-primary auth-submit", style: "width: 100%; justify-content: center;" %>
            </div>
          <% end %>
        </div>
      </div>

      <div class="auth-tab-content hidden" role="tabpanel" id="panel-signup-bsky" aria-labelledby="tab-signup-bsky" data-tabs-target="content" data-tab-name="bsky-oauth">
        <h2 class="auth-section-title">🦋 Sign up with Bluesky</h2>
        <div class="auth-section-body">
          <%= form_tag "/auth/atproto", method: :post, data: { turbo: false } do %>
            <div class="form-group handle-group">
              <%= label_tag :bsky_handle, "Bluesky Handle" %>
              <%= text_field_tag :bsky_handle, nil, class: 'form-control', placeholder: 'e.g. alice.bsky.social', required: true %>
            </div>
            <%= submit_tag "Sign up via Bluesky", class: "sleek-btn sleek-btn-secondary bsky-btn", style: "margin-top: 20px; width: 100%;" %>
          <% end %>
        </div>
      </div>

      <div class="auth-tab-content hidden" role="tabpanel" id="panel-signup-mastodon" aria-labelledby="tab-signup-mastodon" data-tabs-target="content" data-tab-name="mastodon">
        <h2 class="auth-section-title">🐘 Sign up with Mastodon</h2>
        <div class="auth-section-body">
          <%= form_tag "/auth/mastodon", method: :post, data: { turbo: false } do %>
            <div class="form-group server-group">
              <%= label_tag :mastodon_server, "Mastodon Server URL" %>
              <%= text_field_tag :mastodon_server, nil, class: 'form-control', placeholder: 'e.g. mastodon.social', required: true %>
            </div>
            <%= submit_tag "Sign up via Mastodon", class: "sleek-btn sleek-btn-secondary mastodon-btn", style: "margin-top: 20px; width: 100%;" %>
          <% end %>
        </div>
      </div>

    </div>

    <hr class="auth-divider">
    <p class="auth-signup-prompt" style="text-align: center;">
      Already have an account? <%= link_to "Log in!", login_path %>
    </p>
  </div>
</div>
